---
quick_id: 260713-25w
status: complete
---

# Support Ctrl-click multi-track selection

1. Add focused component tests for Ctrl-click add/remove behavior and reducer tests for primary-selection consistency.
2. Implement modifier-click toggling in `FileGrid` and keep `AppState`'s primary track aligned with the selected paths.
3. Run the focused tests and TypeScript typecheck, then review and commit only the scoped files.
