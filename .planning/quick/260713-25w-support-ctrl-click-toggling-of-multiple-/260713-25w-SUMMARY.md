---
quick_id: 260713-25w
status: complete
commit: 5810db4
---

# Ctrl-click multi-track selection summary

- Added Ctrl/Command-click toggling without changing ordinary click or Shift-range selection.
- Kept the primary selected track and cover state consistent when toggling the primary or final selected row off.
- Added intent-focused component and reducer regression tests.

## Verification

- `npx vitest run test/components/FileGrid.test.tsx test/state/app-reducer.test.ts` — 60 tests passed.
- `npm run typecheck` — passed.
- `git diff --check` — passed for all scoped source and test files.
- Scoped diff review found no unused code, duplicated branches, or unhandled selection edge cases.
