# Historical checkpoint fixtures

These 1,578 JSON files are preserved historical outputs from the FLAC doctor
checkpoint pipeline. They moved from the retired `frontend/reports/` tree when
the active Tauri application moved to the repository root.

Runtime reports continue to be written under the ignored root `reports/`
directory. These snapshots are tracked as test data and must not be treated as
live reports.
